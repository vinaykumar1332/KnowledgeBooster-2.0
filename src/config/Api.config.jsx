// src/config/Api.config.jsx
const API_CONFIG = {
    PROXY_URL: "/api/appsProxy", // This works locally + on Vercel

    HEADERS: {
        "Content-Type": "application/json",
    },
};

export default API_CONFIG;